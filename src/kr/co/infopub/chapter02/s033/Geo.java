package kr.co.infopub.chapter02.s033;

// 객체를 이용하여 위도, 경도 표현하기
// 위도, 경도를 객체의 맴버(해당 객체에서 가장 중요한 데이터)로 선언한다.

// (위도, 경도)를 저장하는 클래스
public class Geo {
	public double latitude;		// 자동 초기화
	public double longitude;
}
